{% extends 'base.html.twig' %}

{% block body %}

	<div class="card">

		<div class="photos">

			<img src="{{ ad.image }}" alt="Photo annonce">
			<img src="{{ ad.image }}" alt="Photo annonce">
			<img src="{{ ad.image }}" alt="Photo annonce">

		</div>

		<div class="content">

			<a href=" {{ path('home') }} ">
				<ion-icon class="icon" name="arrow-back-sharp"></ion-icon>
			</a>
			<h2>
				{{ ad.title }}
			</h2>

			publié le
			{{ ad.createdAt | date('d/m/Y') }}
			à
			{{ ad.createdAt | date('H:i') }}
			<div>{{ ad.content }}</div>

		</div>

	</div>

{% endblock %}
